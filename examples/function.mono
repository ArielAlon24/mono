let is_even(n) => {
    if (n % 2 == 0) {
        return True
    }
    return False
}

let main() => {
    
    let index = 1

    while index < 1000 {
        if is_even(index) {
            print(index)
        }
        index = index + 1
    }
}

main()